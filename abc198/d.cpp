#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define REP(i, m, n) for (ll i = (ll)(m); i < (ll)(n); i++)
#define rep(i, n) REP(i, 0, n)

int main()
{
    string s1, s2, s3;
    cin >> s1;
    cin >> s2;
    cin >> s3;

    REP(i, pow(10, s1.size() - 1), pow(10, s1.size()) - 1)
    {
        REP(j, pow(10, s2.size() - 1), pow(10, s2.size()) - 1)
        {
        }
    }
}